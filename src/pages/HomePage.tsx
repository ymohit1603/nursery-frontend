import { Intro } from '../components/Intro';
import { NavBar } from '../components/NavBar';

export const HomePage = () => {
    return (
        <div>
            <NavBar />
            <Intro />
        </div>
    );
};